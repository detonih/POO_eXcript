public class MinhaClasse extends Object implements xyz{
  // Toda vez que definimos uma classe ela vai ser por definição uma extensão da Object
  // Toda classe tem Object como superclass

  
  /* 
  podemos declar 5 componentes de classes diferentes:
  1) modificadores de acesso
  a) public
  b) private
  c) protected
  d) default ou padrão (quando nao definimos qual modificador, sem modificador)
  
  2) atribuir um nome (começa com eltra maíscula)
  3) podemos extender ela de outra (extends)
  4) a inteface que a nossa classe ira implementar (implements)
  5) o corpo da classe (o que esta entre chaves)
  */
  
  //Dentro das chaves das classes temos:
  //campos
  //construtor
  //metodos
  
}